# `assets/` 资源与数据规则

这些规则适用于 `assets/**`。

## 数据文件

- `assets/data/` 优先放 JSON
- 一个文件尽量只负责一个领域，例如战斗、掉落、敌人、经济
- 文件名使用小写和连字符或下划线，保持稳定
- JSON 必须能通过语法校验

## 与设计同步

- 新增调参数据时，要能在设计文档里找到出处
- 如果某个值只在数据里存在、文档里完全没有解释，优先补文档

## 资源组织

- 目录命名优先按系统和用途，而不是按“临时版本”
- 不把废弃版本长期混在正式资源目录里
